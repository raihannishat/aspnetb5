using System;

namespace Delegate
{
    class Program
    {
        static void Main(string[] args)
        {
            //var calculator = new Calculator();
            //Calculator.myDelegate myDelegate = null;
            //myDelegate += calculator.Sum;
            //myDelegate += calculator.Sub;
            //myDelegate += calculator.Mul;
            //myDelegate.Invoke(10, 20);

            //Transaction transaction = new Transaction();
            //transaction.WithdrawTransaction(5000);

            //Action<int, int> action = calculator.Sum;
            //action += calculator.Sub;
            //action += calculator.Mul;
            //action.Invoke(10, 20);

            //Func<int, int, int> func = calculator.Sum;
            //func += calculator.Sub;
            //func += calculator.Mul;
            //func.Invoke(10, 20);
        }
    }
}
